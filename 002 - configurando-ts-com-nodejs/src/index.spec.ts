//import { data } from './index';
describe('File Jest - Test', () => {
  test('Test', () => {
    expect(1).toBe(1);
  });

  // test('Testando', () => {
  //     expect(data).toBe("Caique Gomes")
  //})
});
